{
  "name": "ejs-build",
  "version": "1.1.0",
  "main": "./lib/ejs-build.js",
  "description": "Compiles a passed in EJS template with a supplied context object and spits out results.",
  "bin": {
    "ejs-build": "./ejs-build.js"
  },
  "keywords": [
    "ejs",
    "build",
    "template",
    "context"
  ],
  "scripts": {
    "pretest": "coffee --output lib --bare src/ejs-build.coffee",
    "test": "mocha 'test/**/*.spec.coffee'"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:alexgorbatchev/ejs-build.git"
  },
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "coffee-errors": "^0.8.5",
    "coffee-script": "^1.7.1",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "ejs": "^2.5.5"
  }
}
